# 573-project
Parent repo for our ECE573 project.


Notes: you need to compile riscv64-unknown-linux-gnu via `make linux` after configuring the toolchain appropriately.
You must also run the scripts from the top-level (sorry, no build system).
